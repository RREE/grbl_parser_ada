Tests for the Grbl parser
-------------------------

`test_cl` can send single lines to the parser.  You can see if the handlers are called and generate the expected output

`bbt` uses `test_cl` to compare the the output to the expectations

